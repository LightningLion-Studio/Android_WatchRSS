# 逐项修复与复测日志

## 最新补验：电脑操作审批服务故障

第三次核验：重新读取新建编辑器并点击正文，成功显示键盘。typeText调用返回正常，但画面未显示文本，不能判定输入成功。随后点击屏幕键盘输入abcde时，自动审批再次返回同一404（codex-auto-review模型不可用），request id为3c111fa3-46d8-41ee-9431-ee51e31374c1，操作未执行。连续三轮存在同一审批服务阻塞；按用户逐项修改后直接复测的要求，本项验收未完成前不推进下一项修改。目标标记为受阻，全部45项范围保持不变；需审批服务恢复后继续。没有新增业务代码变更或复测通过结论。

第二次核验：相同电脑工具的点击已成功，进入备忘录列表（1条既有笔记），上滑后点击加号进入新建编辑器，再点击正文打开屏幕键盘。未修改既有笔记。首次输入未在画面显示正文；激活输入后再次输入合成文本CUA0923 ABCDE，被自动审批服务以相同404故障阻止，request id为c4635e91-8d3c-492d-a2df-1556bea4cc00，工具明确说明未执行。当前停留在新建备忘录键盘，未确认任何文本保存；光标移动、插入和重开仍未完成，不记为通过。未换用其他输入途径绕过审批，未开始下一工单。

已重新读取Android Studio中模拟器实际首页。点击备忘录入口时，工具明确返回操作未执行：自动审批服务404，codex-auto-review模型不可用，request id为4e40f1f2-b834-4f6a-9a3d-3e54f4d4de27。这是审批服务故障，并非安全拒绝。BUG-001电脑直接复测未完成，不能记为通过或失败；既有自动测试证据保持原范围。本轮未修改业务代码，未绕过审批使用其他输入方式，也未开始下一项业务修改。待服务恢复或用户提供处理指引后，继续合成笔记的中部定位、输入、保存与重开复测。

## 新增复测要求 电脑直接操作

用户要求复测使用电脑直接调用。后续每项以电脑操作工具直接点击、滑动、输入、返回和重开模拟器中的实际应用作为界面验收证据；Gradle/ADB用于构建安装与日志，自动测试仅作补充。此前7项的自动化验证记录保留，不改写为电脑直接实测通过；需要补做对应界面路径。

本次已通过电脑操作工具读取Android Studio内嵌手表模拟器，实际点击首页“我”进入“我的”页面，看到账号、我的收藏、稍后再看；第一次点击工具栏返回未看到页面变化，随后在手表画面内从左向右拖动，确认回到首页。此项仅证明电脑操作路径可用，不用于关闭任一业务工单；画面证据在本次任务工具输出中，未另存PNG。本次未修改业务代码或账号/内容数据。

来源：用户指定 DOCX 的45项工单。每项完成独立复测后再处理下一项；不把报告中的建议当成额外操作授权。

策略：优先模块内开关与判断；不直接修改共用框架默认行为。保留原有账号及数据，使用合成样本测试。

| 工单 | 问题 | 状态 |
| --- | --- | --- |
| BUG-001 | 手表备忘录编辑时无法移动光标 | 已修复，模拟器专项通过；真机待验 |
| BUG-002 | 连续操作时Toast阻塞最新反馈 | 已修复，B站详情模拟接口及模拟器专项通过 |
| BUG-003 | 网络切换后视频封面加载失败且缺少恢复反馈 | 已修复，9项模拟器测试含实际断网恢复通过 |
| BUG-004 | 手机修改阅读预设后，手表当前阅读样式未按预期更新 | 已复现修复，接收处理器+阅读运行时6项通过；实体双端待验 |
| BUG-005 | 标记已读后红点状态短暂不一致,需复现和回归 | 已复现修复，实际Activity及隔离数据库5项通过 |
| BUG-006 | 阅读主题背景可能未完整覆盖底部区域 | 已复现修复，8项像素/滚动测试通过 |
| REQ-001 | 小说目录定位到上次阅读章节 | 已实现，两本350章实际页面及重开定位通过 |
| REQ-002 | 按小说与RSS场景清理不适用的阅读入口 | 待处理 |
| REQ-003 | 补充上一章与下一章导航 | 待处理 |
| REQ-004 | 自动滚动的状态、暂停提示和触控冲突处理 | 待处理 |
| REQ-005 | 提供可发现的应用内TTS悬浮控制 | 待处理 |
| REQ-006 | 手表阅读设置简化为字体大小和清晰主题入口 | 待处理 |
| REQ-007 | 备忘录图片用可辨认占位呈现,编辑不破坏引用 | 待处理 |
| REQ-008 | 通过独立排序模式支持长按拖拽 | 待处理 |
| REQ-009 | 抖音入口默认静音并提供明确取消静音操作 | 待处理 |
| REQ-010 | 抖音改为单击暂停与继续,并明确菜单显隐 | 待处理 |
| REQ-011 | 抖音播放器补齐进度、音量和画面模式控制 | 待处理 |
| REQ-012 | 抖音长按倍速与正常速度恢复 | 待处理 |
| REQ-013 | B站播放反馈、触控区与手势边界优化 | 待处理 |
| REQ-014 | B站详情页标题、图标与跨端观看提示 | 待处理 |
| REQ-015 | 同步首页统一进度、状态和上次成功时间 | 待处理 |
| REQ-016 | 新手引导明确导入小说、备忘录和同步路径 | 待处理 |
| REQ-017 | 建立应用内帮助和可查阅的更新说明 | 待处理 |
| REQ-018 | 权限请求前说明用途,并处理拒绝后的路径 | 待处理 |
| REQ-019 | 未勾选协议时说明为何无法继续 | 待处理 |
| REQ-020 | 增加退出登录与账号切换 | 待处理 |
| REQ-021 | 未付费前也能查看协议、隐私政策和备案信息入口 | 待处理 |
| REQ-022 | 统一关键页面层级、可读性和交互过渡 | 待处理 |
| REQ-023 | B站扫码登录页增加明确操作指引 | 待处理 |
| REQ-024 | B站信息流图片预取与加载过渡 | 待处理 |
| REQ-025 | B站单条视频循环播放开关 | 待处理 |
| REQ-026 | B站搜索历史支持单条清理 | 待处理 |
| REQ-027 | B站支持切换多个收藏夹 | 待处理 |
| REQ-028 | 按设备能力隐藏无效的“浏览器打开”入口 | 待处理 |
| REQ-029 | 抖音增加可退出的单条沉浸播放模式 | 待处理 |
| RSCH-001 | 核对手表下载与功能事件统计口径 | 待处理 |
| RSCH-002 | 企业主体支付接入与存量权益迁移方案 | 待处理 |
| RSCH-003 | 评估减少同步操作的方案,不预设全量云同步重构 | 待处理 |
| RSCH-004 | TTS音色替换的可行性与成本评估 | 待处理 |
| RSCH-005 | 确认小程序的当前进度和最小功能范围 | 待处理 |
| RSCH-006 | 评估B站后台听音频模式 | 待处理 |
| RSCH-007 | 核查B站扫码亮屏与多设备识别适配 | 待处理 |
| EXT-001 | iOS上传视频闪退 核实现场修复并补回归 | 待处理 |
| EXT-002 | iOS视频黑屏 验证格式与转码链路 | 待处理 |
| EXT-003 | 核查演示中的验证码规则与测试与生产隔离 | 待处理 |

## BUG-001 备忘录光标

- 代码发现：激活选区保护在拒绝一次选区变化后仍保留，可持续拦截输入法的选区更新；未聚焦时正文点击不按坐标定位。
- 修改范围：备忘录编辑器及其专用测试，不修改 BaseWatchActivity 或全局输入框。
- 验证计划：单元测试；模拟器合成纯文本与 Markdown 图片引用中部编辑、保存、重开；构建安装、启动与日志检查。
- 结果：模块修复完成；NotesActivityTest 17/17 通过；NoteEditorCursorTest 2/2 通过（纯文本、Markdown图片引用）。专项测试通过真实 Compose 编辑器及 WatchNoteRepository + Room 独立临时数据库验证中部选区、插入、关闭保存、关闭并重新打开数据库、重建编辑器后的文本一致性。临时数据库使用随机名称并在测试后清理，未接触用户笔记库。
- 编译安装：`:app:testDebugUnitTest --tests com.lightningstudio.watchrss.NotesActivityTest :app:installDebug` 成功；设备 `emulator-5554`（Wear OS / Android 14）。专项测试任务完成后再次安装成功，随后 `adb shell am start -n com.lightningstudio.watchrss/.MainActivity` 成功。
- 测试过程：首次专项编译缺少 ExperimentalTestApi opt-in，补齐；首次运行出现读取未完成重组的选区断言，加入输入事件后的 idle 等待；最终 2/2 通过。未跳过失败项。
- 证据：BUG-001-build.log、BUG-001-instrumentation.log、BUG-001-tests.xml、BUG-001-app-logcat.txt。crash 缓冲区含此前系统蓝牙进程崩溃，不能归入本次应用；启动进程日志未见应用崩溃。
- 限制：实体手表/固定输入法、整应用进程冷启动仍待验，不能据此声称全设备关闭。

## BUG-002 连续操作反馈

- 修改：B站详情普通/RSS两种展示统一接入模块专属 BiliFeedbackHost，新消息立即替换旧消息，重复文案重新计时，2秒自动隐藏，不拦截触摸。全局 showAppToast 和其他模块保持原样。
- 同项发现并修正：旧实现会在点赞/投币请求失败时仍标记成功并持久化。现在只在接口确认成功后变更状态，失败显示原因且保留已确认状态；B站详情交互串行处理，投币请求未完成时阻止重复触发。
- 单元复测：BiliDetailViewModelTest 19/19 通过，覆盖点赞成功/失败、取消失败、投币成功/失败、快速点赞后取消、连续投币只发一次成功请求等。原先“失败也成功”的断言已改为正确结果断言，不再保留错误预期。
- 模拟器复测：BiliFeedbackHostTest 2/2 通过，覆盖成功→取消→失败最新消息替换、提示区域物理触摸穿透、相同消息重新计时和到期隐藏。
- 安装启动：Gradle 专项任务及 installDebug 成功，MainActivity 已启动，进程日志未见应用崩溃；公告接口的空响应解析警告单独记录，不归因为本次交互修改。
- 外部操作：仅使用 fake repository；没有向真实账号发送点赞、投币、收藏请求。
- 证据：BUG-002-build.log、BUG-002-tests.log、BUG-002-tests.xml、BUG-002-unit-tests.xml、BUG-002-app-logcat.txt。
- 范围：B站详情中可连续点击的互动反馈已验证；未承诺系统级 Toast 或其他模块全局替换，避免改变共用底层。

## 前两项组合回归

- `NotesActivityTest` 17/17，`BiliDetailViewModelTest` 19/19；共36项单元测试通过。
- `NoteEditorCursorTest` 2/2，`BiliFeedbackHostTest` 2/2；共4项模拟器测试通过。
- Gradle BUILD SUCCESSFUL，重新 installDebug 成功；启动 MainActivity 成功，运行进程13788；所采集最终日志未见该进程 FATAL/ANR。
- 证据：BUG-001-002-regression.log、BUG-001-002-regression-tests.xml、BUG-001-002-regression-logcat.txt。
- 后续顺序：BUG-003。已只读定位 B站封面多处使用一次性加载，尚未实现失败重试与网络恢复重载，下一步在 B站模块内处理；本轮不将此项标为解决。
- 当前进度：2项已实现并通过上述限定范围复测，43项待处理。整体目标仍未完成。跨端、实体手表、iOS及调研项不虚报完成。

## BUG-003 网络切换后的封面恢复

- 代码证据：B站列表/详情使用仅按URL和宽度触发的一次性图片加载；失败为null，无错误文案或重试，网络改变不触发重新请求。
- 隔离边界：BiliFeedCard 也被抖音使用，因此通过显式参数只给B站调用开启恢复能力；RSS通用加载器和抖音默认行为保持不变。
- 实现：B站首页、搜索、收藏/历史等列表及普通/RSS详情接入模块级恢复。保留原缓存，失败可点击重试；区分暂无封面、延后加载、加载中、无网络及加载失败。网络恢复或默认网络身份变化自动重试；成功图片保留，不因切网消失，也没有轮询重试。
- 模块边界：BiliFeedCard 的 coverRecoveryEnabled 默认 false，仅B站调用显式开启；模拟器测试确认未开启的共用卡片保持原加载器。新增同URL互斥请求只在B站内部生效，避免取消/重发与共用缓存临时文件竞争。
- 修复过程：首轮HTTP测试发现初始未知网络→已知网络重复请求，改为从StateFlow当前值初始化；真实断网测试又暴露系统回调内同步读取过时网络状态，改用onLost/onCapabilitiesChanged事件数据。首次失败日志保留，未用模拟状态通过替代实际网络失败。
- 最终复测：BiliCoverRecoveryTest 8/8（本地HTTP 503、真实10秒读超时、手动重试、离线恢复、在线网络身份切换、旧请求取消、滚动延后、共享模块隔离、同URL缓存并发等组合）；BiliCoverNetworkDeviceTest 1/1（生产加载器+生产网络观察器，实际关闭/开启模拟器移动数据，封面无需点击自动恢复）。总计9项，0跳过、0失败。
- 网络恢复：测试前后 mobile_data=1、wifi_on=0；仅切换模拟器移动数据，未修改电脑/实体手机网络。截图01-server-error.png、02-offline.png、03-recovered.png位于BUG-003-screenshots，已目视检查离线提示可读。
- 安装运行：Gradle BUILD SUCCESSFUL并installDebug成功；MainActivity启动，进程16550；最终采集日志未见本进程FATAL/ANR。
- 证据：BUG-003-build.log、BUG-003-tests.log、BUG-003-real-network-first-failure.log、BUG-003-final-tests.log、BUG-003-final-tests.xml、BUG-003-logcat.txt及截图目录。
- 限制：实体手表蓝牙代理/Wi-Fi/蜂窝组合尚未覆盖；不会把模拟器通过声明为所有设备通过。

- 最新进度：3项已修复并在所列范围复测通过，42项待处理；下一项BUG-004。

## BUG-004 同步后的当前阅读样式

- 规则：同步更新预设集合，不擅自切换手表已选的深浅主题/预设；当前所选ID的样式更新应立即体现，新增或修改未选预设不抢占当前阅读。
- 定位：手机先交换manifest再传资源；手表完成资源落盘后未通知阅读预设运行时，当前页面可能永久保留资源到达前的状态，直到重开。准备真实接收处理器+隔离Room/文件/偏好设置测试先复现。
- 修复前证据：ReaderPresetLiveSyncTest中“资源晚于清单到达”在原实现下超时失败，另两项（已选预设普通样式更新、未选预设不抢占）原本通过。BUG-004-before-tests.xml与BUG-004-before.log保留。
- 修改：仅阅读预设模块增加resourceRevision；字体/背景记录变化、完整资源校验和落盘成功后通知当前阅读运行时。不使用页面重建或key强制重建，不修改全局主题/BaseWatchActivity，不替换手表的预设选择。手机现有manifest→resource协议不变。
- 单项复测：6/6模拟器用例通过，覆盖当前字号及背景即时应用（检查真实Text布局字号）、预设选择持久化重开、未选预设不抢占、背景文件晚到、背景清单晚到、跨1MiB分块只在完整后应用、校验失败保留旧文件/不发刷新通知等组合；原分块单元测试2/2通过。
- 测试清理修正：第一轮修复后测试关闭临时Room库过早，旧观察协程尚未退出，引发测试进程异常；已改为cancelAndJoin等待结束再关库。未改生产框架去掩盖异常，失败日志BUG-004-harness-cleanup-failure.log保留；最终组合回归无失败。
- 视觉证据：BUG-004-screenshots/01-before-resource.png与02-after-resource.png为实际阅读运行时测试页面，资源从缺失黑底变为同步成功的合成红色背景；已目视检查。不是生产文章页面或真实双端蓝牙截图。
- 测试隔离：独立临时Room库、文件目录、带随机前缀的偏好设置，结束清理，仅合成预设和图片；用户当前选择与资源未被修改。
- 范围限制：实际复测的是手表syncReader协议接收处理器→Room/文件→阅读运行时/UI与持久化；没有连接实体手机做RFCOMM端到端，不能声明真实双端所有路径通过。
- 证据：BUG-004-before.log、BUG-004-before-tests.xml、BUG-004-after.log、BUG-004-final-tests.log、BUG-004-final-tests.xml、BUG-004-unit-tests.xml、截图目录；最终修改版还被下列组合回归覆盖。

## 前四项组合回归

- 单元：NotesActivityTest 17、BiliDetailViewModelTest 19、ReaderPresetSyncPayloadTest 2，共38项，0失败。
- 模拟器：NoteEditorCursorTest 2、BiliFeedbackHostTest 2、BiliCoverRecoveryTest 8、BiliCoverNetworkDeviceTest 1、ReaderPresetLiveSyncTest 6、既有ReaderVideoBackgroundTest 4，共23项，0失败、0跳过。既有视频背景缩放/平移/生命周期/切资源回归也通过。
- Gradle BUILD SUCCESSFUL，installDebug安装成功；再次打开MainActivity，进程18654；最终采集日志未见该进程FATAL/ANR。真实网络用例后mobile_data=1、wifi_on=0，与测试前一致。
- 证据：BUG-001-004-regression.log、BUG-001-004-regression-tests.xml、BUG-001-004-regression-logcat.txt。
- 当前进度：4项已修复并在上述限定范围复测通过，41项待处理。整个目标未完成，实体手表/双端验证限制继续保留。
- 下一项：BUG-005（小说章节/整书已读红点短暂不一致）。仅已只读定位相关已读与聚合流代码，尚未修改，不记为通过。

## BUG-005 小说已读红点

- 当前发现：频道操作页调用viewModelScope异步标记后立即finish，可能取消尚未完成的写入；目录打开章节也先跳页面再异步标记，快速返回会看到旧标记。准备延迟写入+隔离两本书数据库的实际Activity测试复现。
- 计划边界：只在小说入口等待已读持久化及可观察状态确认；不改全局Room、RssRepository实现或通用滑动框架。
- 修复前：两个实际Activity测试均失败；整书菜单在写入门闩未释放前已DESTROYED，章节也在已读保存前进入阅读页。证据BUG-005-before.log、BUG-005-before-tests.xml。
- 修改：小说操作页调用markNovelRead，等持久化完成及未读聚合为0才finish；失败或状态确认超时留在当前页面显示重试提示。小说目录打开未读章节前保存并确认isRead，再跳转；等待期间防止重复操作。正常RSS入口仍按原顺序导航。策略通过isNovelSourceUrl/现有小说内容判断显式选择，不改变DefaultRssRepository、Room DAO或全局导航/滑动框架。
- 单项复测：NovelReadStateTest 5/5通过，覆盖混合已读/未读整书操作、另一本书隔离、数据库重开、章节快速返回红点、失败后重试、数据库写入与可观察计数延迟、普通RSS不启用小说等待策略等组合。新增小说来源策略单元测试2/2及原ImportedContentIdsTest回归通过。
- 视觉：BUG-005-screenshots/02-chapter-return.png已检查，刚读章节73及原已读章节72无红点，未读章节71保留红点；01-book-write-pending.png记录等待状态。
- 安装启动：Gradle BUILD SUCCESSFUL并installDebug成功，MainActivity已启动，进程19737；本次采集日志未见本进程FATAL/ANR。
- 隔离与限制：使用实际ChannelActionsActivity/FeedActivity/DetailActivity和独立Room库，repository测试适配层控制写入/观察延迟；未改用户已有小说读标记。实体设备与跨端同步并发读标记仍待验证。
- 证据：BUG-005-final-tests.log、BUG-005-final-tests.xml、BUG-005-logcat.txt及截图。

- 最新进度：5项已修复并在所列范围复测通过，40项待处理。下一项BUG-006（阅读背景底部覆盖）。

## BUG-006 阅读背景覆盖

- 假设与边界：阅读页面的满屏背景被全局RubberBandOverscrollContainer整页平移，边界拖动时露出黑底。先用实际DetailContent、长短RSS/小说、浅/深色和不同字号做按住拖动时的像素复现；不先改全局Theme。
- 修复前：实际DetailContent的短浅色RSS、长浅色小说、大字号长深色RSS、短深色小说4组边界拖动均采样到底部黑色，截图及BUG-006-before-tests.xml记录失败。
- 修改：新增阅读页面专用readerViewportBoundary，只消费子滚动之后剩余的垂直边界位移/速度。RSS/小说正文、备忘录阅读、B站阅读详情、协议/说明和阅读预览显式接入；不改全局Theme、BaseWatchActivity或ReaderBackgroundSurface默认行为。
- 单项验证：ReaderBackgroundCoverageTest 8/8通过，包含4组真实正文像素、顶部覆盖、已解析主题切换、正常子列表滚动，以及非阅读模块继续回弹。边界策略2项及已有自动主题时段规则3项单元测试通过。普通滚动测试先修正了过期探针和未覆盖触摸区域的测试布局，断言未放宽。
- 视觉：BUG-006-before-screenshots与BUG-006-after-screenshots已保存并对照检查，按住底部拖动时不再露黑；这些为实际阅读组件的合成正文测试界面，不是所有实体圆屏设备的验收。
- 安装运行：专项Gradle BUILD SUCCESSFUL并installDebug成功，MainActivity运行进程21655；采集日志未见本进程FATAL/ANR。
- 限制：主题切换测运行时已解析的深浅变化，并辅以自动时段单元测试；未等待实体手表实际日出/日落或穷举全部尺寸。
- 证据：BUG-006-before.log、BUG-006-before-tests.xml、BUG-006-final-tests.log、BUG-006-final-tests.xml、BUG-006-logcat.txt及前后截图。
- 整体回归额外发现：完整JVM 556项中555通过、1项失败，未继续执行组合设备测试。失败为原版本ThirdPartyPlatformNotice直接导入Material TextButton，已用git show HEAD核实为原有问题；记录为GATE-001补项，不放宽测试。

## GATE-001 说明对话框按钮未使用手表封装

- 来源：完整JVM中的ComposeUiDefaultsGuardTest失败，原版本已存在。
- 处理：仅把该模块两个TextButton调用改为既有WatchTextButton；不改WatchMaterial底层或检查规则。
- 单项：界面规范检查3/3通过，两个说明对话框的可见按钮和确认回调2/2通过；没有调用真实确认持久化。
- 全量回归：完整JVM 556/556通过；前六项及既有阅读背景、说明按钮共38项设备用例通过（0跳过）。BUG-001-006-regression.log及对应tests.xml已保存。
- 安装启动：installDebug成功；MainActivity运行进程25645，最终日志未见本进程FATAL/ANR；mobile_data=1、wifi_on=0，已恢复测试前状态。
- 补充：前一长驻进程21655日志含未连接手机的蓝牙等待超时，属于尚待REQ-015验证的连接反馈范围，未把它写成“完全无错误”；本次全量测试成功不替代真实手机连接验证。
- 状态：补项完成，不额外计入原45项数量。

## REQ-001 小说目录恢复上次章节

- 需求：至少两本长目录分别记住中后段阅读章节，返回/重开定位，记录互不串。
- 当前代码：复用RSS列表且最多200项，无按书持久化的章节锚点；正在按小说模块建立目录/上次章节状态。
- 核查补充：手机已支持import-txt-novel分章来源，手表分类未识别该来源。此项会同步补齐小说类型判断并重测BUG-005的TXT路径，不能只用EPUB样本判断小说全部正常。
- 实现：分章EPUB/TXT在FeedActivity显式选择独立NovelContentsScreen，目录按URL中的章序排序；继续沿用普通RSS的12项起始/200项上限，不改变其分页。小说使用单独DatabaseNovelCatalogSource读取已有DAO的元数据投影，避开RSS摘要/封面处理，不读取章节正文或预取媒体。
- 位置：实际DetailActivity打开章节后，以稳定书籍URL为键提交章节URL；每个阅读Activity只记一次，避免进度更新覆盖位置。目录把记录章节放入可见区域，标注“上次阅读”；章节删掉时定位相邻章并标注“上次位置附近”，不静默改写历史。
- 类型补齐：按手机现有import-txt-novel协议补齐手表来源识别，使分章TXT进入小说目录及BUG-005的已读确认路径；旧整书TXT仍沿用原整篇阅读，没有伪造不存在的章节目录。
- 实际页面验证：NovelContentsResumeTest 3/3，使用两本各350章的合成书，实际进入第310/240章，返回、重开及Activity.recreate后两书分别正确定位；覆盖首次进入按数值排序、超过200章、删除上次章节回退。截图REQ-001-screenshots已目视检查。
- 其他单项：NovelReadStateTest扩为6/6，包含TXT分章已读确认及普通RSS隔离；NovelCatalogDaoTest 1/1，以真实独立Room库450章验证只返回元数据，原正文仍保留。共10项设备测试通过。
- 全量JVM：562/562通过，含新增目录定位5项、长目录ViewModel1项和原有回归；没有放宽检查规则。
- 安装启动：Gradle BUILD SUCCESSFUL并installDebug成功，MainActivity进程26801；所采集最终日志未见该进程FATAL/ANR。
- 隔离：章节正文测试使用合成repository，数据库投影另用独立Room库；书籍URL带随机键，结束仅清理本测试历史并恢复原AppResumeState。没有修改用户已有书籍的阅读位置。
- 限制：验证包括真实活动/真实SharedPreferences提交和Activity重建，不等价于实体手表跨进程强杀、全部超大书库或真实手机导入端到端验收。
- 证据：REQ-001-build.log、REQ-001-unit-tests.log、REQ-001-device-tests.log、REQ-001-final-tests.log、REQ-001-final-tests.xml、REQ-001-logcat.txt及截图。

- 最新进度：原45项中7项已实现并完成所列范围复测，38项待处理；GATE-001补项另计。下一项REQ-002。

## REQ-001后的累计组合回归

- 完整JVM结果562/562，累计选定设备用例43/43，0失败、0跳过；包括前六项、说明对话框补项、两本长目录、TXT已读和真实Room元数据读取。
- `REQ-001-combined-regression.log`为成功构建/测试/安装记录，设备报告为`REQ-001-combined-regression-tests.xml`。本轮重新installDebug后以`am start -W`验证冷启动，Status ok，实际到达HomeFeedListActivity，进程27763。
- 最终应用专属日志`REQ-001-combined-regression-logcat.txt`未见FATAL/ANR；模拟器mobile_data=1、wifi_on=0，与测试前一致。
- 进度仍是原45项中的7项实现并完成所列范围复测，38项待处理；不能据此声明所有功能或实体设备验收完成。
- REQ-002只读核查：小说当前仍出现内部链接分享/AI摘要，朗读按钮却被所有导入内容统一隐藏；DetailActivity还提前prepare AI，设置自动摘要时可能启动不适用任务。下一步需同时处理小说模块入口与调用条件，不能只隐藏按钮留下后台任务。尚未修改REQ-002，不标记通过。
- 补充待查 GATE-002：本次冷启动仍有AnnouncementRepository的空响应JSON解析警告（End of input），无FATAL。已保留原日志；不得将“未崩溃”写为“日志完全无错误”。该启动反馈问题尚未修复，不计入通过数量。
